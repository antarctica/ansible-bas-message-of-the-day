---
# Default variables - inventory or playbook overrides will be respected

motd_information_infrastructure_provider: ""
motd_information_infrastructure_manager: ""

motd_information_distribution_name: "{{ ansible_distribution }}"
motd_information_distribution_version: "{{ ansible_distribution_version }}"

motd_information_template_name: "{{ ansible_local.os_template.general.name }}"
motd_information_template_version: "{{ ansible_local.os_template.general.version }}"

motd_information_systems_inventory_identifer: ""
motd_information_systems_inventory_resource: ""
